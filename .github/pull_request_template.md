## Summary

<!-- What changed and why. -->

## Screenshots / recording

<!--
For any user-facing/UI change: attach a "before" and "after" screenshot, or
just the current view if there's no meaningful "before" (a new page, for
example). For a multi-step flow or an interaction a still image can't convey
(a drag, an animation, a modal sequence), attach a short screen recording
instead. Skip this section entirely for backend-only or non-visual changes.
-->

## Test plan

<!--
How this was verified — e.g. `yarn type-check`, `yarn lint`, `yarn test`,
`yarn e2e`, `yarn docs:build`, and any manual steps.
-->
